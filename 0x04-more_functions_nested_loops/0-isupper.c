#include "main.h"

/**
 * _isupper - upppercase letters.
 * @c: Char to be check.
 *
 * Return: 1 if uppercase
 * zero if otherwise.
 */
int _isupper(int c)
{
	if ((c >= 'A') && (c <= 'z'))
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return ('\n');
}
